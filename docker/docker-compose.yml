version: '3.4'
services:
#  Optional
#  disable-auditing-aspect-share:
#    image: ${docker.share.image}:${alfresco.share.version}
#    environment:
#      REPO_HOST: disable-auditing-aspect-acs
#      REPO_PORT: 8080
#    ports:
#      - "${share.port}:8080"
  disable-auditing-aspect-acs:
    image: alfresco-content-services-disable-auditing-aspect:development
    build:
      dockerfile: ./Dockerfile
      context: ../../../target
    environment:
      CATALINA_OPTS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=0.0.0.0:8888"
    ports:
      - "${acs.port}:8080"
      - "${acs.debug.port}:8888"
    volumes:
      - disable-auditing-aspect-acs-volume:/usr/local/tomcat/alf_data
    depends_on:
      - disable-auditing-aspect-postgres
  disable-auditing-aspect-postgres:
    image: postgres:9.6
    environment:
      POSTGRES_DB: alfresco
      POSTGRES_USER: alfresco
      POSTGRES_PASSWORD: alfresco
    command: postgres -c max_connections=300 -c log_min_messages=LOG
    ports:
      - "${postgres.port}:5432"
    volumes:
      - disable-auditing-aspect-db-volume:/var/lib/postgresql/data
  disable-auditing-aspect-ass:
    image: alfresco/alfresco-search-services:1.2.0
    environment:
      SOLR_ALFRESCO_HOST: disable-auditing-aspect-acs
      SOLR_ALFRESCO_PORT: 8080
      SOLR_SOLR_HOST: disable-auditing-aspect-ass
      SOLR_SOLR_PORT: 8983
      SOLR_CREATE_ALFRESCO_DEFAULTS: alfresco,archive
    ports:
      - "8983:8983"
    volumes:
      - disable-auditing-aspect-ass-volume:/opt/alfresco-search-services/contentstore
      - disable-auditing-aspect-ass-volume:/opt/alfresco-search-services/data
volumes:
  disable-auditing-aspect-acs-volume:
    external: true
  disable-auditing-aspect-db-volume:
    external: true
  disable-auditing-aspect-ass-volume:
    external: true